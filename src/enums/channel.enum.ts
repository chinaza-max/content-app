// enums/channel.enum.ts
export enum RouteType {
  SMS = "sms",
  WHATSAPP = "whatsapp",
  EMAIL = "email",
}

export enum MonetizationType {
  FREE = "free",
  PAID = "paid",
}

export enum ChannelStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
